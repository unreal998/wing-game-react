import { Box, styled } from "@mui/material";
import { MAIN_COLORS } from "../../../shared/colors";

export const TableBoxHead = styled(Box)({
  fontWeight: 700,
  color: MAIN_COLORS.textColor,
  paddingTop: "9px",
  paddingBottom: "5px",
  width: "100%",
});
