import { Stack, styled } from "@mui/material";

export const StackWallet = styled(Stack)(({ theme }) => ({
  flexDirection: "column",
  padding: "7px 20px",
  color: "white",
  alignItems: "center",
  justifyContent: "center",
  gap: "20px",
}));
