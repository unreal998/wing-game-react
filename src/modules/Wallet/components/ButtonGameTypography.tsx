import { styled, Typography } from "@mui/material";
import { MAIN_COLORS } from "../../../shared/colors";

export const ButtonGameTypography = styled(Typography)({
  color: "rgb(0, 0, 0)",
  fontSize: "16px",
  fontWeight: 600,
});
