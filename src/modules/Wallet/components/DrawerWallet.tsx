import { Drawer, styled } from "@mui/material";

export const DrawerWallet = styled(Drawer)({
  "& .MuiDrawer-paper": {
    margin: "0px 10px",
    borderRadius: "10px",
    backgroundColor: "#031C2B",
  },
});
