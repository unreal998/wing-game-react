import { Stack, styled } from "@mui/material";

export const StackWalletStyle = styled(Stack)(({ theme }) => ({
  flexDirection: "row",
  justifyContent: "space-around",
  width: "100%",
  paddingBottom: "35px",
}));
