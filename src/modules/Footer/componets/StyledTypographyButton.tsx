import { styled, Typography } from "@mui/material";

export const StyledTypographyButton = styled(Typography)({
  color: "rgb(0, 0, 0)",
  fontSize: "20px",
  fontWeight: 600,
  fontStyle: "italic",
});
