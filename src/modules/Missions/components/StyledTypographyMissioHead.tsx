import { styled, Typography } from "@mui/material";
import { MAIN_COLORS } from "../../../shared/colors";

export const StyledTypographyMissioHead = styled(Typography)({
  fontSize: "12px",
  fontWeight: 700,
  paddingTop: "10px",
  paddingBottom: "10px",
  color: MAIN_COLORS.textColor,
});
