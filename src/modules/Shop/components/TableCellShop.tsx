import { styled, TableCell } from "@mui/material";
import { MAIN_COLORS } from "../../../shared/colors";

export const TableCellShop = styled(TableCell)({
  border: 0,
  color: MAIN_COLORS.textColor,
  fontSize: "13px",
  padding: "8px",
});
